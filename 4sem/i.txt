fd = open("fifo", )
read(fd, )
write(fd, )
close(fd)

system call:
getcvd
chdir

shell:
pwd
cd

file
удаление файла 
unlink (syscall) - удаляет одно из имен конкретного фалйа
а удаление файла как такогвого происходит, когда на этот объект исчезнет последняя внешняя ссылка (что этот файл использует)
1)имя файла в файловой системе
2)открытый удаленный файл (откроем файл и удалим его, он существует, но open выдаст ошибку

создать второе имя файла - link отвзать unlink
жесткие ссылки (link) можно делать только на файлы, но не на директории
но существует концепция мягких ссылок
специальный файл, содержащий имя файла или директории к которой нужно перейти
(глубина дерева имен ограничена и ветвь мягких ссылоек тоже ограничена)
соpдание мягких ссылок (не считаются как счетчик использования):
ln -S
syscall symlink

file
clenteg
inode

stat
fstat

opendir 
readdir
closedir

Смена режима дескриптора ONBLOCK ONDELAY
Либо уже на коткрытом файловом дескрипторе можно вызвать syscall fcntl

dup
dup2 - создание копии дескриптора в текущем процессе

В случае тредов:
Если писать в один файловый дескриптор в двух тредах => мешанина
Поэтому
pread
pwrite

readv
writev (vector)
preadv
pwritev

mkdir
rmdir

rename - переименовать файл в пределах одной директории
- atomic system call

В Unix все пространство имен единое 
все имена идут от \
mount монтирует корень другой директории в специальную директорию
При этом та, вместо которой мы маунтим - исчезает
потом после анмаунта она появится вновь

